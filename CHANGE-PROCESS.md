# 修改迭代与发布流程（强制规范）

> 版本：2.0 ｜ 2026-09-12
> 适用范围：本仓库的一切改动——代码、资源、依赖、文档、**发布物**。没有"这次很小所以免了"的例外。
>
> 历史起因：
> - **v1.0（2026-09-08）**：v0.2.0 前出现四个回归（心语识别失效、让它提个时候闪退、日历未写入、照片不可见），
>   每一条都能在本文档的某一关被拦下。
> - **v2.0（2026-09-12）**：v0.2.2 暴露 v1.0 的结构性漏洞——**流程只管"代码对不对"，不管"用户装到的包是不是这份代码"**。
>   同一版本号下，`release/` 里的 APK 比源码落后一次改动；发布 tag 不存在，已有的 v0.2.0 tag 树里也没有 v0.2.0 的包；
>   v1.0 的 G5 还引用了一张并不存在的"README 版本表"。证据与复盘见 **附录 C**。
>
> v2.0 的核心变化：① 按变更级别路由关卡；② 把交付物一致性做成**可机械校验**的关卡（`scripts/preflight.sh`）；
> ③ 提交与留痕规范化，**版本更新必须 git add + git commit + tag，且在同一次操作内完成**。

---

## 零、红线（违反即不得提交 / 不得发布）

| 编号 | 红线 | 拦截关卡 |
|---|---|---|
| R1 | **交付物漂移**：`release/` 里的 APK 落后于同版本主源码（APK 入库后又并入了 `app/src/main` 改动） | C6（机械） |
| R2 | **同版本号重发不同内容的包**，或改了行为却不递增版本号 | C6（机械）+ 附录 A 台账 |
| R3 | 发布 tag 缺失，或 tag 树里**取不到**该版本的 APK | C6（机械） |
| R4 | 基线回归清单（附录 A）中仍有 `⚠️ 未验证` / `🔧 待重验` 项即发布 | C3（人工确认） |
| R5 | 提交信息不合规（非 conventional commits，或缺少附录 D 的必需段落） | C8 + pre-commit 钩子 |
| R6 | **混提交**：一次 commit 同时包含 功能 / 重构 / 改名 / 版本号 / 发布物 中两类以上 | C8（评审） |
| R7 | 未跑机械校验就提交或发布 | pre-commit 钩子（强制） |
| R8 | 改了字符串 / 常量 / 列名 / 路径 / 契约，却未给出 grep 影响面 | C8（commit 正文必须含"影响面"） |
| R9 | 构建产物、`local.properties`、`.idea/`、`.claude/`、非 `release/*.apk` 的包被入库 | C1（机械） |

> 红线不是"尽量"，是"必须"。**当前仓库处于 R1 + R3 的存量违规状态**（见附录 C），
> 必须在下一个版本 v0.2.3 修复；修复前不得对外声称 v0.2.2 的包与源码一致。

## 一、总原则

1. **回归优先**：改动的第一风险不是"新功能不对"，而是"旧功能坏了"。每轮验收里旧功能的权重不低于新功能。
2. **无自动化测试的路径 = 高危路径**：v0.2.0 的四个回归全部落在没有自动化覆盖的路径上。不肯写测试的路径，将来一定在这里返工。
3. **改"名字/文案/结构"也要当功能改**：日历未写入那条回归就来自"改个应用名"——查询条件跟着名字改了一半。任何字符串、常量、列名、路径的修改必须全局搜索引用点。
4. **验证必须闭环到用户可见的效果**：编译通过、单测通过、启动无崩溃都不等于功能正常。"日历写入成功"的唯一标准是系统日历里出现那条事件。
5. **交付物是代码的一部分**：版本号、APK、tag、台账四者必须描述同一份代码。四者不一致 = 这个版本不存在。
6. **无留痕 = 未验证**：验证结论必须写进 commit 正文或台账，含设备 / ROM / 日期 / 证据。口头"我测过了"不算通过。

## 二、变更分级与关卡路由

开工前先定级，按级别过关；**不得事后补定级**。

| 级别 | 触发条件 | 必过关卡 |
|---|---|---|
| **L0 文档** | 仅 `.md` / 注释 / 无运行时影响 | C1 → C8 |
| **L1 应用代码** | 主源码行为变更（UI / 逻辑 / 文案 / 依赖） | C1 → C2 → C3（本项 + 受影响项） → C8 |
| **L2 数据与外部契约** | Room 实体或列或版本、备份格式、系统日历写入、网络契约 | L1 全部 **+ C4 + C5** |
| **L3 版本发布** | 改 `versionCode` / `versionName`、换 `release/*.apk`、打 tag | L2 全部 **+ C6 + C7** |

判定有歧义时**就高不就低**。一次会话只推进一级，跨级改动必须拆成多次提交与多轮验收。

## 三、修改前（Pre-flight）

### P1. 复现与记录基线
- 改任何已存在功能前，在当前版本**实际操作一遍**该功能并记录预期表现（截图或文字）。
- 同时记录基线三要素：`versionName`、当前 commit hash、测试设备/ROM。
- 无法复现的现状 = 无法验证的修改。先搞清现状，再动手。

### P2. 影响面清单（必须写进 commit 正文）
- 全局搜索被改的**每一个标识符 / 字符串 / 文件路径**的所有引用点，并把命令与命中数写下来：
  ```bash
  grep -rn "旧标识符" android/app/src android/app/schemas .three-tomato/requirements *.md
  ```
- 改 UI 文案 → 搜该文案在代码、资源、提示词、文档中的全部出现处。
- 改数据结构（Room 实体/表列）→ 列出所有读写该结构的位置。
- 改外部交互（日历 / 文件 / 网络）→ 列出全部调用方与降级路径。

### P3. 变更分级
- 按第二章定级，写进 commit 正文的"级别"字段。

### P4. 回归清单登记
- 列出"本次改动可能碰坏的既有功能"（对照附录 A），这份清单是发布前的必查项，不是可选项。

## 四、实现中（Discipline）

### D1. 小步提交
- 一次 commit 只做一件事。混合"功能 + 重构 + 改名"的提交是回归温床。
- 重构（纯结构调整）与行为变更（哪怕一行）不得同 commit。

### D2. 新代码必须有可观测性
- 涉及外部系统（日历 / 文件 / 模型接口）的路径必须打日志（`Log.d` 起步）。
- 日历路径全程静默曾让 P3 的排查耗时翻倍。日志 tag 统一 `Windveil<模块>`（如 `WindveilCal`）。

### D3. 解析外部输入必须走严格工具
- LLM 返回一律经 `LlmJson` 解析，禁止 `removePrefix/removeSuffix` 手搓。
- Room / JSON 字段缺失必须显式默认值，不依赖平台行为。

### D4. UI 变更的可用性下限
- 触发关键动作（保存/确认/删除）的按钮不得小于 48dp，主操作建议整行宽。
- 保存类操作必须有可见反馈（Toast / 状态变化）。无反馈 = 用户以为失败 = 重复操作 = 数据异常。

### D5. 提交即落盘（版本更新强制）
- 每完成一个**可独立验证的最小单元**，立刻 `git add <显式路径>` + `git commit`。改动不得留在工作区跨会话、跨天。
- **每次版本更新（改版本号 / 换 APK / 打 tag）必须在同一次操作内完成 `git add` + `git commit` + `git tag`**，不允许"文件改好了但没提交"。
- **禁止 `git add -A` / `git add .` / `git commit -a`**：必须显式列路径。本仓库 `release/*.apk` 是 `.gitignore` 的例外，更要显式 add。
- 提交后 `git status` 必须干净（除已知忽略项）。

### D6. 数据库与 schema 同步
- 改 Room 实体必须同时：① 写 `MIGRATION_x_y`；② 导出/新增 `android/app/schemas/<n>.json`；③ 让 `@Database(version = n)` 与 schema 文件名一致。
- 三者缺一，C4 直接拦下。

## 五、提交与版本更新规范

### 5.1 提交信息
模板见 **附录 D**。必需段落：类型前缀 + 标题、`变更`、`影响面`、`验证`；发布提交追加 `版本`。
只写一句"V0.2.2"的裸提交属于 R5，不得出现。

### 5.2 版本更新的标准动作序列（逐字执行，不许跳步）

```bash
# 1) 递增版本号：android/app/build.gradle.kts 的 versionCode + versionName（两者都要 +1/改号）
# 2) 构建发布包（当前发布形态 = debug 签名；release 变体未配置签名，产物不可安装，不得入库）
cd android && ./gradlew :app:assembleDebug && cd ..
# 3) 入库交付物（显式路径；文件名即版本）
cp android/app/build/outputs/apk/debug/app-debug.apk "release/风起簿-vX.Y.Z.apk"
sha256sum "release/风起簿-vX.Y.Z.apk"        # 记进提交正文与台账
# 4) 更新 README「发布记录」台账（版本 / 日期 / 交付物 / versionCode / tag / sha256）
# 5) 跑发布关卡
sh scripts/preflight.sh release
# 6) 提交 + 打 tag（tag 必须指向这个包含 APK 的 commit）
git add android/app/build.gradle.kts "release/风起簿-vX.Y.Z.apk" README.md
git commit
git tag -a vX.Y.Z -m "vX.Y.Z：<一句话>"
```

### 5.3 tag 规则
- 用附注 tag（`git tag -a`），指向**包含该版本 APK 的那个 commit**（即上文的 release commit）。
- tag 不得指向版本号提交之前的 commit——v0.2.0 的教训：tag 落在 `6f20d3a`（只改版本号），
  而 v0.2.0 的 APK 在下一个 commit 才入库，导致 tag 树里根本没有 0.2.0 的包（附录 C）。

### 5.4 交付物不可变
- 已发布的 `release/风起簿-vX.Y.Z.apk` 内容不得再变；要改行为就发新版本号。
- 历史版本文件可以从工作区删除以省体积，但必须能从对应 tag 取回：`git show vX.Y.Z:"release/风起簿-vX.Y.Z.apk" > /tmp/old.apk`。
- 台账里的 sha256 是"用户装到的包"的唯一指纹，报障复现时先对指纹。

## 六、发布前验收关卡（不过不放行）

### C1. 机械卫生检查
```bash
sh scripts/preflight.sh quick
```
- 检查是否有构建产物 / 本地配置被暂存或入库；检查版本号、schema、台账是否自洽。

### C2. 自动化测试全绿
```bash
cd android && ./gradlew :app:testDebugUnitTest
```
- 新增行为必须带新测试（对齐已有 `TimingCalculatorTest` 的写法）。
- 修 bug 必须先写"复现该 bug 的测试"再修（至少修完后补上）。

### C3. 核心路径手工回归（附录 A 清单逐项过）
在模拟器或真机上按清单实际操作，每项确认后打勾，**用眼睛看结果，不凭代码推断**。
每项测完在 commit 正文或台账写下：设备 + ROM + 日期 + 结果。本轮必测路径：

1. 种愿望 → 出现 AI 追问（或降级提示）
2. 心语说"我想去滑雪" → 记入未发生之地
3. 心语说"今晚取快递" → 记入随手记
4. 心语问"明天中午吃什么" → 给出建议（引用记录/画像）
5. 愿望详情 → 让它提个时候 → 不闪退 → 采纳/拒绝
6. 定节假日/日期时机 → 确认弹窗 → **系统日历应用里实际出现事件**
7. 随手记编辑 → 加照片 → 保存 → **卡片上实际看到缩略图**
8. 记忆页收进书里 → 跳转书架
9. 导出 JSON → 清数据 → 导入 JSON → 数据回来（照片 base64 恢复）
10. 设置改模型配置 → 出现"已保存"提示 → 心语用新配置
11. 愿望详情「和它聊聊」→ 发一条 → 杀进程重进 → 气泡仍在（DB v5 聊天历史）

### C4. 数据库迁移核对
- `@Database(version =)` == `app/schemas/` 里最大编号 == 迁移链终点，三处一致。
- 改实体必须有对应 `MIGRATION_x_y`，且迁移 SQL 与 schema JSON 描述的结构一致。

### C5. 升级安装测试
- **不卸载直接装新版本**（保留旧数据）：Room 迁移必须无崩溃、旧数据完整。
- 至少覆盖一个"跨两个版本升级"的路径（如 v0.2.0 → 最新），不只用上一版。

### C6. 交付物一致性（v2.0 新增，机械校验）
```bash
sh scripts/preflight.sh release
```
五处必须指向同一版本：`build.gradle.kts` 的 `versionName`、`release/` 里的 APK 文件名、
README 台账最新行、`git tag`、tag 树内确实存在该 APK。
另外校验：**APK 入库之后不得再有 `app/src/main` 改动**（R1 交付物漂移）。

### C7. 崩溃扫描与指纹记录
```bash
adb logcat -d | grep -E "FATAL EXCEPTION"
sha256sum "release/风起簿-vX.Y.Z.apk"
```
- 崩溃清零才放行；sha256 写进台账与提交正文。

### C8. 提交与台账落盘
- 发布提交本身要符合附录 D，正文含版本、sha256、验证记录。
- 提交后 `git status` 干净、tag 已打、台账已更新。三者缺一视为未发布。

## 七、发布后（Post-release）

- 用户报告的每个问题先**在与用户完全相同的 APK（同一 sha256）上复现**。曾在旧构建上白白排查——先确认测试对象与用户一致。
- 修复合入后按级别重过全部关卡，**版本号必须递增**，哪怕只改了一行。
- 发布后 24 小时内回填台账的"验证"列与附录 A 的最后验证版本/commit。

---

## 附录 A：基线回归清单（随功能演进维护）

状态图例：`✅ 已验证` ｜ `🔧 待重验` ｜ `⚠️ 未验证`

| # | 路径 | 级别 | 最后验证版本 | 最后验证 commit | 证据 / 备注 |
|---|------|------|------------|----------------|------------|
| 1 | 种愿望 + AI 追问 | L1 | v0.2.0 | — | 2026-09-08 手工回归 |
| 2 | 心语记愿望（wish 分流） | L2 | v0.2.0 | — | 2026-09-08 手工回归 |
| 3 | 心语记随手记（lite 分流） | L2 | v0.2.0 | — | 2026-09-08 手工回归 |
| 4 | 心语建议（记忆+画像排序） | L2 | v0.2.0 | — | 2026-09-08 手工回归 |
| 5 | 让它提个时候（不闪退） | L1 | v0.2.0 | — | 2026-09-08 手工回归 |
| 6 | 时机 → 系统日历事件 | L2 | — | — | ⚠️ 无真机验证记录（部分 ROM 对 `ACCOUNT_TYPE_LOCAL` 行为不一） |
| 7 | 随手记照片保存 + 显示 | L2 | — | — | ⚠️ 无真机验证记录 |
| 8 | 收进书里 → 跳书架 | L1 | v0.2.0 | — | 2026-09-07 手工回归 |
| 9 | 导出 / 导入 JSON | L2 | v0.2.0 | — | 2026-09-08 手工回归，v2 格式 |
| 10 | 模型配置保存提示 | L1 | v0.2.0 | — | 2026-09-07 手工回归 |
| 11 | 愿望对话历史持久化（DB v5） | L2 | — | — | ⚠️ v0.2.2 起并入，**发布包中不含此功能**（附录 C） |

> **当前状态（2026-09-12）**：第 6、7、11 项为 `⚠️ 未验证`。按 R4，此刻不具备发布条件；
> v0.2.3 发布前必须补齐真机验证并回填"最后验证 commit"。

## 附录 B：v0.2.0 四回归根因与对应关卡

| 问题 | 根因 | 本应在哪一关拦下 |
|------|------|----------------|
| 1. 心语识别失效 | LLM 返回解析脆弱（后被 `LlmJson` 修复）+ prompt 迭代 | C3-2/3/4 手工回归 |
| 2. 让它提个时候闪退 | 边界未防御（v0.2.0 已修） | C3-5 + C7 |
| 3. 日历未写入 | 改名"风起簿"时日历查询条件只改一半（`=` 双参数失效） | P2 影响面清单：改名必须 grep 全部引用 |
| 4. 照片不可见 | 保存按钮窄易点空 + 复制/保存链路无反馈无日志 | D4 可用性下限 + D2 可观测性 |

## 附录 C：v0.2.2 交付物漂移复盘（v2.0 的直接起因）

| # | 事实（可复核） | 违反 |
|---|---------------|------|
| 1 | 提交 `3fee484`（2026-09-12 22:15）信息仅 `V0.2.2`，无正文；一个 commit 里混入 DB v4→v5 迁移、`chat_messages` 新表、`StandaloneRepository`、`AnalysisService`、`LiteEventsScreen`、`WishDetailScreen` 与 README，共 9 个文件 | R5、R6 |
| 2 | `release/风起簿-v0.2.2.apk` 最后修改于 `b51133f`（2026-09-09 11:10），此后 `3fee484` 又改了 `app/src/main`，而 `versionName` 仍是 `0.2.2` | R1、R2 |
| 3 | 校验方法：解包该 APK 的 `classes*.dex` 搜索字符串 `chat_messages`，命中数 0 → 该包不含 DB v5 与聊天历史功能；而仓库源码 `@Database(version = 5)` 已含之。即**用户装到的包 ≠ 仓库里的代码** | R1 |
| 4 | tag `v0.2.0` 指向 `6f20d3a`（只改版本号），其树中的发布物是 `风起簿-v0.1.0.apk`；`v0.2.0.apk` 在下一个 commit `ae094b3` 才入库 → tag 树取不到本版本交付物 | R3 |
| 5 | 无 `v0.2.1` / `v0.2.2` tag；README 无版本表，而 v1.0 的 G5 要求"与 README 版本表同 commit"——规则引用了一张不存在的表，从未可执行 | R3、v1.0 缺陷 |
| 6 | 本地 `app-debug.apk` 最后构建于 2026-09-11 23:56（体积 17784435），与入库的 v0.2.2 包（17746999）不同 → 新代码构建过但没入库、没升版本号 | R1、R2 |

**v2.0 的对应措施**：C6 把第 2/3/4 项变成机器可判的检查；附录 A 台账要求 sha256 与验证 commit；
第 5 项由 README 新增的「发布记录」台账修复；第 6 项由 D5"提交即落盘 + 版本更新必须 add/commit/tag"约束。

## 附录 D：commit message 模板

```
<type>(<scope>): <一句话标题，动词开头，≤ 50 字>

级别：L0 | L1 | L2 | L3
版本：vX.Y.Z（仅发布提交填写；含 versionCode / versionName）
变更：
- 做了什么（可验证的行为变化，不写"优化了一下"）
影响面：
- grep 命令 + 命中数：`grep -rn "xxx" android/app/src` → 3 处
- 改动的读写方 / 调用方 / 降级路径
验证：
- 单测：`./gradlew :app:testDebugUnitTest` 全绿（新增 N 个用例）
- 手工：设备/ROM + 日期 + 逐项结果（对照附录 A 编号）
- 交付物：sha256 = <...>（发布提交）
风险与回滚：
- 已知风险 + 出问题怎么退回（如 `git revert <sha>`）
```

`type` 取值：`feat` / `fix` / `refactor` / `perf` / `docs` / `chore` / `test`。
`scope`：`android`、`process`、`release`、`book`、`calendar`、`heart-voice`、`backup` 等模块名。

## 附录 E：发布台账

唯一权威版本台账在 [`README.md`](README.md) 的「发布记录」章节，本文件不重复维护。
`scripts/preflight.sh` 会核对台账、源码版本号与 `release/` 目录是否自洽。
