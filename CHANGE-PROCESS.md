# 修改迭代升级流程（强制规范）

> 版本：1.0 ｜ 2026-09-08
> 起因：v0.2.0 前的迭代出现四个回归（心语识别失效、让它提个时候闪退、日历未写入、照片不可见）。
> 每一条都能在本文档的某一关被拦下。此后**任何代码修改必须走完本流程**，无例外。

---

## 一、总原则

1. **回归优先**：改动的第一风险不是"新功能不对"，而是"旧功能坏了"。每轮迭代的验收清单里，旧功能占的权重不低于新功能。
2. **无自动化测试的路径 = 高危路径**：本次四个回归全部落在没有自动化覆盖的路径上（日历写入、照片保存、心语解析、闪退边界）。凡是不肯写测试的路径，将来一定在这里返工。
3. **改"名字/文案/结构"的修改也要当功能改**：P3（日历未写入）正是"改个应用名"引入的——一个查询条件跟着名字改了一半。任何字符串、常量、列名、路径的修改必须全局搜索引用点。
4. **验证必须闭环到用户可见的效果**：编译通过、单测通过、启动无崩溃，都不等于功能正常。"日历写入成功"的唯一标准是系统日历里出现那条事件，不是代码看起来会写。

## 二、修改前（Pre-flight）

### P1. 复现与记录基线
- 改任何已存在功能前，先在当前版本**实际操作一遍该功能**并记录预期表现（截图或文字）。
- 无法复现的现状 = 无法验证的修改。先搞清现状，再动手。

### P2. 影响面清单（必须写进 commit 或提案）
- 全局搜索被改的**每一个标识符/字符串/文件路径**的所有引用点（`grep -rn`）。
- 改 UI 文案 → 搜该文案在代码、资源、提示词、文档中的全部出现处。
- 改数据结构（Room 实体/表列）→ 列出所有读写该结构的位置。
- 改外部交互（日历/文件/网络）→ 列出全部调用方与降级路径。

### P3. 回归清单登记
- 每轮修改开工前，把"本次改动可能碰坏的既有功能"列成清单（见附录 A 基线清单）。
- 这份清单是发布前的必查项，不是可选项。

## 三、实现中（Discipline）

### D1. 小步提交
- 一次 commit 只做一件事。混合"功能 + 重构 + 改名"的提交是回归温床。
- 重构（纯结构调整）与行为变更（哪怕一行）不得同 commit。

### D2. 新代码必须有可观测性
- 涉及外部系统（日历/文件/模型接口）的路径必须打日志（`Log.d` 起步）。
- 本次 P3 排查耗时翻倍的原因就是日历路径全程静默。日志 tag 统一 `Windveil<模块>`（如 `WindveilCal`）。

### D3. 解析外部输入必须走严格工具
- LLM 返回一律经 `LlmJson` 解析，禁止 `removePrefix/removeSuffix` 手搓。
- Room/JSON 字段缺失必须显式默认值，不依赖平台行为。

### D4. UI 变更的可用性下限
- 触发关键动作（保存/确认/删除）的按钮不得小于 48dp，主操作建议整行宽。
- 保存类操作必须有可见反馈（Toast/状态变化），无反馈 = 用户以为失败 = 重复操作 = 数据异常。

## 四、发布前验收（Gate —— 不过不放行）

### G1. 自动化测试全绿
```bash
cd android && ./gradlew :app:testDebugUnitTest
```
- 新增行为必须带新测试（对齐已有 `TimingCalculatorTest` 的写法）。
- 修 bug 必须先写"复现该 bug 的测试"再修（TDD 修正案：至少修完后补上）。

### G2. 核心路径手工回归（附录 A 清单逐项过）
在模拟器或真机上按清单实际操作，每项确认后打勾。**用眼睛看结果，不凭代码推断。**
本轮必测路径（后续每轮按功能追加）：
1. 种愿望 → 出现 AI 追问（或降级提示）
2. 心语说"我想去滑雪" → 记入未发生之地
3. 心语说"今晚取快递" → 记入随手记
4. 心语问"明天中午吃什么" → 给出建议（引用记录/画像）
5. 愿望详情 → 让它提个时候 → 不闪退 → 采纳/拒绝
6. 定节假日/日期时机 → 确认弹窗 → **系统日历应用里实际出现事件**
7. 随手记编辑 → 加照片 → 保存 → **卡片上实际看到缩略图**
8. 记忆页收进书里 → 跳转书架
9. 导出 JSON → 清数据 → 导入 JSON → 数据回来（照片 base64 恢复）
10. 设置改模型配置 → 出现"已保存"提示 → 心语用新配置

### G3. 升级安装测试
- **不卸载直接装新版本**（保留旧数据）：Room 迁移必须无崩溃、旧数据完整。
- 检查 `app/schemas/` 版本号与迁移代码一致。

### G4. 崩溃扫描
```bash
adb logcat -d | grep -E "FATAL EXCEPTION"
```
- 清零才放行。

### G5. 发布物更新
- 通过全部 Gate 后：更新 `release/风起簿-vX.Y.Z.apk` 与 README 版本表同 commit。
- 版本号 `versionCode`/`versionName` 必须递增，禁止同版本号覆盖发不同内容的包。

## 五、发布后（Post-release）

- 用户报告的每个问题先**在最新 release APK 上复现**（教训：本轮我曾在旧构建上白白排查——先确认测试对象版本与用户一致）。
- 修复合入后按 G1–G5 重新过闸，哪怕只改了一行。

---

## 附录 A：基线回归清单（随功能演进维护）

| # | 路径 | 状态 | 最后验证 |
|---|------|------|---------|
| 1 | 种愿望 + AI 追问 | ✅ v0.2.0 | 2026-09-08 |
| 2 | 心语记愿望（wish 分流） | ✅ v0.2.0 | 2026-09-08 |
| 3 | 心语记随手记（lite 分流） | ✅ v0.2.0 | 2026-09-08 |
| 4 | 心语建议（记忆+画像排序） | ✅ v0.2.0 | 2026-09-08 |
| 5 | 让它提个时候（不闪退） | ✅ v0.2.0 | 2026-09-08 |
| 6 | 时机 → 系统日历事件 | 🔧 修复中（IN 查询） | 待重验 |
| 7 | 随手记照片保存+显示 | 🔧 加固中（整行保存按钮） | 待重验 |
| 8 | 收进书里 → 跳书架 | ✅ | 2026-09-07 |
| 9 | 导出/导入 JSON | ✅ v2 格式 | 2026-09-08 |
| 10 | 模型配置保存提示 | ✅ | 2026-09-07 |

## 附录 B：本轮四问题的根因与对应关卡

| 问题 | 根因 | 本应在哪一关拦下 |
|------|------|----------------|
| 1. 心语识别失效 | LLM 返回解析脆弱（后被 LlmJson 修复）+ prompt 迭代 | G2-2/3/4 手工回归 |
| 2. 让它提个时候闪退 | 边界未防御（v0.2.0 已修） | G2-5 + G4 |
| 3. 日历未写入 | 改名"风起簿"时日历查询条件只改一半（`=` 双参数失效） | P2 影响面清单：改名必须 grep 全部引用 |
| 4. 照片不可见 | 保存按钮窄易点空 + 复制/保存链路无反馈无日志 | D4 可用性下限 + D2 可观测性 |
